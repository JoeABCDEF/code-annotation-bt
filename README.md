实现一个 TypeScript 并发任务调度库，用于限制大量异步任务同时执行。需要支持最大并发数、任务排队、优先级、暂停/恢复、动态修改并发数、任务超时、AbortSignal 取消、运行状态统计和等待全部任务结束。需正确处理任务异常、同步任务、队列中取消、运行中取消、超时后任务仍未真正结束、并发数动态降低、相同优先级顺序、大量任务同时提交等边界情况。不能出现超出并发限制、任务重复执行、任务丢失、队列卡死或异常后并发槽无法释放。禁止使用现成的并发调度库，并提供完整自动化测试验证上述行为。
